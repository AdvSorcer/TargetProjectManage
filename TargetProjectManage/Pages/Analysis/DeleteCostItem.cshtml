@page
@model TargetProjectManage.Pages.Analysis.DeleteCostItemModel
@{
    ViewData["Title"] = "刪除成本項目";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <div>
        <h1><i class="bi bi-trash me-2"></i>刪除成本項目</h1>
        @if (Model.CostItem.Project != null)
        {
            <div class="text-muted">
                <i class="bi bi-folder me-1"></i>@Model.CostItem.Project.ProjectName 
                <span class="badge bg-primary ms-2">@Model.CostItem.Project.ProjectCode</span>
            </div>
        }
    </div>
    <a asp-page="CostAnalysis" asp-route-projectId="@Model.CostItem.ProjectId" class="btn btn-outline-secondary">
        <i class="bi bi-arrow-left me-1"></i>返回成本分析
    </a>
</div>

<div class="row">
    <div class="col-md-8">
        <div class="card border-danger">
            <div class="card-header bg-danger text-white">
                <h5><i class="bi bi-exclamation-triangle me-2"></i>確認刪除</h5>
            </div>
            <div class="card-body">
                <div class="alert alert-danger">
                    <h4 class="alert-heading">警告！</h4>
                    <p>您即將刪除此成本項目，此操作無法復原。請確認您要刪除的項目：</p>
                </div>

                <div class="card">
                    <div class="card-header">
                        <h6><i class="bi bi-calculator me-2"></i>成本項目詳細資訊</h6>
                    </div>
                    <div class="card-body">
                        <div class="row">
                            <div class="col-md-6">
                                <strong>項目名稱：</strong>
                                <p>@Model.CostItem.ItemName</p>
                            </div>
                            <div class="col-md-6">
                                <strong>成本類別：</strong>
                                <p><span class="badge bg-secondary">@Model.CostItem.Category</span></p>
                            </div>
                        </div>
                        
                        <div class="row">
                            <div class="col-md-6">
                                <strong>預估成本：</strong>
                                <p>NT$ @Model.CostItem.EstimatedCost.ToString("N0")</p>
                            </div>
                            <div class="col-md-6">
                                <strong>實際成本：</strong>
                                <p>NT$ @Model.CostItem.ActualCost.ToString("N0")</p>
                            </div>
                        </div>
                        
                        @if (!string.IsNullOrEmpty(Model.CostItem.Description))
                        {
                            <div class="mb-3">
                                <strong>說明：</strong>
                                <p>@Model.CostItem.Description</p>
                            </div>
                        }
                        
                        @if (!string.IsNullOrEmpty(Model.CostItem.ContractNumber))
                        {
                            <div class="row">
                                <div class="col-md-6">
                                    <strong>合約編號：</strong>
                                    <p>@Model.CostItem.ContractNumber</p>
                                </div>
                                <div class="col-md-6">
                                    <strong>付款條件：</strong>
                                    <p>@Model.CostItem.PaymentTerms</p>
                                </div>
                            </div>
                        }
                        
                        @if (Model.CostItem.PaymentDate.HasValue)
                        {
                            <div class="mb-3">
                                <strong>付款日期：</strong>
                                <p>@Model.CostItem.PaymentDate.Value.ToString("yyyy/MM/dd")</p>
                            </div>
                        }
                        
                        <div class="mb-3">
                            <strong>建立日期：</strong>
                            <p>@Model.CostItem.CreatedDate.ToString("yyyy/MM/dd HH:mm")</p>
                        </div>
                    </div>
                </div>

                <form method="post">
                    <input type="hidden" asp-for="CostItem.Id" />
                    <div class="d-flex justify-content-between">
                        <a asp-page="CostAnalysis" asp-route-projectId="@Model.CostItem.ProjectId" class="btn btn-secondary">
                            <i class="bi bi-x-circle me-1"></i>取消
                        </a>
                        <button type="submit" class="btn btn-danger">
                            <i class="bi bi-trash me-1"></i>確認刪除
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
    
    <div class="col-md-4">
        <div class="card">
            <div class="card-header">
                <h5><i class="bi bi-info-circle me-2"></i>刪除影響</h5>
            </div>
            <div class="card-body">
                <div class="alert alert-warning">
                    <h6><i class="bi bi-exclamation-triangle me-2"></i>刪除此項目將影響：</h6>
                    <ul class="mb-0">
                        <li>專案總成本統計</li>
                        <li>成本結構分析圖表</li>
                        <li>成本控制率計算</li>
                        <li>財務分析報表</li>
                    </ul>
                </div>
                
                <hr>
                
                <h6>建議替代方案：</h6>
                <ul>
                    <li>將實際成本設為 0 而非刪除</li>
                    <li>在說明中標記為「已取消」</li>
                    <li>保留歷史記錄以便追蹤</li>
                </ul>
                
                <div class="mt-3">
                    <a asp-page="EditCostItem" asp-route-id="@Model.CostItem.Id" class="btn btn-outline-primary btn-sm">
                        <i class="bi bi-pencil me-1"></i>編輯而非刪除
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>